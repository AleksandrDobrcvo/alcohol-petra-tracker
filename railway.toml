[build]
builder = "nixpacks"

[deploy]
startCommand = "chmod +x scripts/build.sh && ./scripts/build.sh && npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"
NEXT_TELEMETRY_DISABLED = "1"
